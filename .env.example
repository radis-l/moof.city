# =============================================================================
# moof.city - Environment Configuration
# =============================================================================
# Copy to .env.local and fill in your values
# NEVER commit .env.local (it's git-ignored)

# Database (Supabase - used for both local and production)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# Authentication
# Local dev: use "admin" as password
# Production: managed via admin page (stored in database)
ADMIN_PASSWORD=admin
JWT_SECRET=generate_a_random_32_char_string

# Analytics (Optional)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
