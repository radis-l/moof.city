# Fortune Tell Website - Environment Configuration Template
# Copy this file to .env.local and fill in your values

# Google Analytics 4 Configuration (Optional)
# Get your measurement ID from Google Analytics dashboard
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Production Configuration (.env.local)
# Supabase Database (Production)
# Get these from your Supabase project dashboard
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
USE_SUPABASE_PRIMARY=true

# Local Development Configuration (.env.local)
# Admin password for development environment
ADMIN_PASSWORD=secure_dev_password_123

# JWT Secret for authentication tokens
JWT_SECRET=super_secret_jwt_key_change_in_production

# Development Mode Settings
# USE_SUPABASE_PRIMARY=false
# # SUPABASE_URL=  (commented out for dev)
# # SUPABASE_ANON_KEY=  (commented out for dev)

# Storage System Overview:
# 🏠 Local Development: SQLite database (data/local.db)
#    - Fast, isolated, no internet required
#    - Perfect for development and testing
#    - Uses ADMIN_PASSWORD environment variable
# 🌍 Production: Supabase (scalable cloud database)
#    - Reliable, backed up, handles traffic
#    - Stores hashed admin password in database
# 🔐 Security: Both environments now use bcrypt hashing
#    - Development: ADMIN_PASSWORD hashed in memory
#    - Production: Password stored hashed in Supabase
