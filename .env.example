# Google Analytics 4 Configuration
# Get your measurement ID from Google Analytics dashboard
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Vercel KV Configuration (Production only)
# These are automatically provided by Vercel in production
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Development Notes:
# - In development, the app uses local JSON file storage in data/ directory
# - In production (Vercel), it automatically switches to Vercel KV storage
# - Admin password is stored in data/admin-config.json (development) or Vercel KV (production)
